# you-supply-algorithm
The algorithm to match orders and create delivery routes for the youSupply system

# Running

Run this command in your terminal at the root directory to execute the driver code:

```bash
python -m Driver_Code.driver
```